rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for now to fix authentication issues
    // In production, you should implement proper security rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}